# 如何运行打包脚本

## 方法 1：使用 CMD（推荐）

1. 打开 CMD（命令提示符）
2. 进入项目目录：
   ```cmd
   cd D:\xiafeng\Project\Python\STM32_FLASH
   ```
3. 运行脚本：
   ```cmd
   build_package.bat
   ```

## 方法 2：使用 PowerShell

1. 打开 PowerShell
2. 进入项目目录：
   ```powershell
   cd D:\xiafeng\Project\Python\STM32_FLASH
   ```
3. 运行 PowerShell 脚本：
   ```powershell
   .\build_package.ps1
   ```

   如果遇到执行策略错误，运行：
   ```powershell
   Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
   .\build_package.ps1
   ```

## 方法 3：双击运行

直接双击 `build_package.bat` 文件即可。

## ⚠️ 注意

- **不要在 PowerShell 中直接运行 .bat 文件**
- 如果在 PowerShell 中，请使用 `.\build_package.ps1`
- 如果在 CMD 中，请使用 `build_package.bat`

## 常见错误

### 错误 1: "'o' 不是内部或外部命令"

**原因**: 在 PowerShell 中运行了 .bat 文件

**解决**:
- 使用 CMD 运行 .bat 文件
- 或在 PowerShell 中运行 .ps1 文件

### 错误 2: "无法加载文件，因为在此系统上禁止运行脚本"

**原因**: PowerShell 执行策略限制

**解决**:
```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

### 错误 3: "未找到虚拟环境"

**原因**: 虚拟环境未创建

**解决**:
```cmd
python -m venv .venv
```
